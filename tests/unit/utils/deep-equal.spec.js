const expect = chai.expect;

// var isObject = (v) => {
//     var type = typeof v;
//     return !!v && type === "object" || type === "function";
// }
//
// var isDeepEqual = (v1, v2) => {
//     // v1 and v2 have same value and type
//     if(v1 === v2){
//         return true;
//     }
//
//     //for cases:
//     //null and undefined, not equal
//     //NaN, equal
//     //two symbols, not equal
//     if(v1 == null || v2 == null || !isObject(v1) && !isObject(v2)){
//         return v1 !== v1 && v2 !== v2;
//     }
//
//
// }

describe('isDeepEqual', () => {
    it('shoud deeply compare two values/objects', () => {
        debugger
        _.isArray([])
    })
})
